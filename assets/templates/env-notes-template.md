# 环境说明（<测试方标识> · <本地环境 / 网络测试环境>）

> 生效日期；环境模式：<本地环境 / 网络测试环境>（自动推断依据：<用户明示 / URL host <值> / 提供本地项目路径>）；中途变更（换端口、重建库、模式纠正）在此追加记录。

## 服务拓扑

| 组件 | 地址 | 说明 |
| --- | --- | --- |
| 前端 | http://<host:port> | 启动方式、代理指向 |
| 后端 | http://<host:port> | 版本/commit、启动方式、日志位置 |
| 数据库 | <实例与库名> | 迁移/基线 + seed 版本 |
| 网络环境注意事项 | （网络测试环境时：其他使用者、数据口径、禁止操作） |

## 数据库构建过程（fresh 库可重建）

1. <建库>
2. <迁移/基线执行方式与序号>
3. <seed 及已知补丁（跳过段落、冲突处理）>
4. <冒烟验证结果：关键表计数>

## 账号清单（密码一律 `***`）

| 账号 | 登录方式 | 角色/用途 |
| --- | --- | --- |
| | | |

## 预检结果（开工日期）

端口 ☐ 前端可达 ☐ 后端健康（版本 <x>）☐ 数据就绪 ☐ 账号可登录 ☐ 浏览器会话干净 ☐ 网络/console 可观察 ☐ 网络通道（VPN/代理/证书，网络测试环境）☐

## 工具能力备注

<如：file chooser 不可用；console 可监听。详见 tool-limits.md>
